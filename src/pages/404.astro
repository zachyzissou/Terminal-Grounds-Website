---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
    title="404 - Lost in the Cascade | Bloom" 
    description="The page you're looking for has been consumed by reality distortion. The IEZ Cascade has corrupted this data stream. Navigate to safety immediately."
    keywords="404 error, page not found, Bloom Terminal Grounds, Cascade interference"
    noindex={true}
    type="website">
    
    <main class="error-page quantum-grid min-h-screen flex items-center justify-center relative overflow-hidden">
        <div class="container relative z-10">
            <div class="error-content text-center max-w-4xl mx-auto">
                <!-- Error Code -->
                <div class="error-code mb-8">
                    <h1 class="holographic-text text-9xl md:text-[12rem] font-display font-black leading-none mb-4">404</h1>
                    <div class="glitch-text text-2xl md:text-4xl font-display font-bold text-secondary mb-6">
                        SIGNAL LOST
                    </div>
                </div>
                
                <!-- Terminal Window -->
                <div class="terminal-window glass-morphism-advanced mb-8 max-w-2xl mx-auto">
                    <div class="terminal-header flex items-center justify-between px-6 py-4 border-b border-border/30">
                        <div class="flex gap-2">
                            <div class="w-3 h-3 bg-red-500 rounded-full"></div>
                            <div class="w-3 h-3 bg-yellow-500 rounded-full"></div>
                            <div class="w-3 h-3 bg-green-500 rounded-full"></div>
                        </div>
                        <span class="text-text-muted text-sm font-mono">cascade_navigation.exe</span>
                    </div>
                    <div class="terminal-body p-6 font-mono text-sm">
                        <div class="terminal-line text-primary mb-2">
                            $ attempting_route_calculation...
                        </div>
                        <div class="terminal-line text-red-400 mb-2">
                            [ERROR] Reality distortion detected
                        </div>
                        <div class="terminal-line text-red-400 mb-2">
                            [ERROR] Navigation path corrupted by Cascade interference
                        </div>
                        <div class="terminal-line text-yellow-400 mb-2">
                            [WARN] Recommend immediate extraction to known coordinates
                        </div>
                        <div class="terminal-line text-primary mb-4">
                            $ initializing_emergency_protocols...
                        </div>
                        <div class="terminal-line text-green-400">
                            [OK] Safe routes identified. Choose extraction point:
                        </div>
                    </div>
                </div>
                
                <!-- Error Message -->
                <div class="error-message mb-8">
                    <h2 class="text-3xl md:text-4xl font-display font-bold text-primary mb-4">
                        Lost in the Cascade
                    </h2>
                    <p class="text-xl text-text-secondary leading-relaxed max-w-2xl mx-auto">
                        The page you're looking for has been consumed by reality distortion. 
                        The IEZ Cascade has corrupted this data stream. Navigate to safety immediately.
                    </p>
                </div>
                
                <!-- Navigation Options -->
                <div class="navigation-options grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-12">
                    <a href="/" class="nav-option interactive-card glass-morphism-advanced p-6 text-center hover:shadow-glow transition-all duration-300">
                        <div class="nav-icon text-4xl mb-3">🏠</div>
                        <h3 class="text-lg font-display font-bold text-primary mb-2">Command Center</h3>
                        <p class="text-text-muted text-sm">Return to base operations</p>
                    </a>
                    <a href="/factions" class="nav-option interactive-card glass-morphism-advanced p-6 text-center hover:shadow-glow transition-all duration-300">
                        <div class="nav-icon text-4xl mb-3">⚔️</div>
                        <h3 class="text-lg font-display font-bold text-secondary mb-2">Faction Database</h3>
                        <p class="text-text-muted text-sm">Access faction intelligence</p>
                    </a>
                    <a href="/regions" class="nav-option interactive-card glass-morphism-advanced p-6 text-center hover:shadow-glow transition-all duration-300">
                        <div class="nav-icon text-4xl mb-3">🗺️</div>
                        <h3 class="text-lg font-display font-bold text-accent-cyan mb-2">Territory Maps</h3>
                        <p class="text-text-muted text-sm">Navigate contested zones</p>
                    </a>
                    <a href="/concept-art" class="nav-option interactive-card glass-morphism-advanced p-6 text-center hover:shadow-glow transition-all duration-300">
                        <div class="nav-icon text-4xl mb-3">🎨</div>
                        <h3 class="text-lg font-display font-bold text-accent-purple mb-2">Asset Archives</h3>
                        <p class="text-text-muted text-sm">View visual documentation</p>
                    </a>
                </div>
                
                <!-- Emergency Actions -->
                <div class="emergency-actions flex flex-col sm:flex-row gap-4 justify-center items-center">
                    <button onclick="history.back()" class="cyber-button px-8 py-3">
                        ← Emergency Retreat
                    </button>
                    <a href="/" class="px-8 py-3 bg-transparent border-2 border-primary/30 text-primary font-display font-bold transition-all duration-300 hover:border-primary hover:shadow-glow hover:bg-primary/10 rounded">
                        Establish Safe Route
                    </a>
                </div>
                
                <!-- Cascade Effect Simulation -->
                <div class="cascade-effects mt-16">
                    <div class="effect-description text-center max-w-2xl mx-auto">
                        <h3 class="text-xl font-display font-bold text-text-muted mb-4">Reality Distortion Analysis</h3>
                        <div class="distortion-data grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
                            <div class="data-point">
                                <div class="data-value text-2xl font-display font-bold text-red-400">∞</div>
                                <div class="data-label text-text-muted">Dimensional Drift</div>
                            </div>
                            <div class="data-point">
                                <div class="data-value text-2xl font-display font-bold text-yellow-400">404ms</div>
                                <div class="data-label text-text-muted">Signal Latency</div>
                            </div>
                            <div class="data-point">
                                <div class="data-value text-2xl font-display font-bold text-accent-purple">N/A</div>
                                <div class="data-label text-text-muted">Stability Index</div>
                            </div>
                            <div class="data-point">
                                <div class="data-value text-2xl font-display font-bold text-primary">SAFE</div>
                                <div class="data-label text-text-muted">Extraction Status</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <style>
        .glitch-text {
            position: relative;
            animation: glitch 2s infinite;
        }
        
        .glitch-text::before,
        .glitch-text::after {
            content: attr(data-text);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }
        
        .glitch-text::before {
            animation: glitch-1 0.5s infinite;
            color: #ff6b35;
            z-index: -1;
        }
        
        .glitch-text::after {
            animation: glitch-2 0.5s infinite;
            color: #00ff88;
            z-index: -2;
        }
        
        @keyframes glitch {
            0%, 100% { transform: translate(0); }
            20% { transform: translate(-2px, 2px); }
            40% { transform: translate(-2px, -2px); }
            60% { transform: translate(2px, 2px); }
            80% { transform: translate(2px, -2px); }
        }
        
        @keyframes glitch-1 {
            0%, 100% { transform: translate(0); }
            20% { transform: translate(2px, -2px); }
            40% { transform: translate(-2px, 2px); }
            60% { transform: translate(-2px, -2px); }
            80% { transform: translate(2px, 2px); }
        }
        
        @keyframes glitch-2 {
            0%, 100% { transform: translate(0); }
            20% { transform: translate(-2px, -2px); }
            40% { transform: translate(2px, -2px); }
            60% { transform: translate(2px, 2px); }
            80% { transform: translate(-2px, 2px); }
        }
        
        .nav-option:hover .nav-icon {
            transform: scale(1.1);
            transition: transform 0.3s ease;
        }
        
        @media (prefers-reduced-motion: reduce) {
            .glitch-text,
            .glitch-text::before,
            .glitch-text::after {
                animation: none;
            }
        }
    </style>
</BaseLayout>